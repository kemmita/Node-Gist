1. In this file we will create functions that will be used in a different js file.

const getNotes = () => {
  return 'Static Note method for my example.';
};

const addNote = (title, body) =>{

};
module.exports = {
    getNotes: getNotes,
    addNote: addNote
};


2. In this file we need to require that file and then access the method we wish to use.

const notes = require('./notes');
console.log(notes.getNotes());
