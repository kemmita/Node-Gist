const book = {
  title: 'The Germanic Tribes',
  author: 'Ausemove'
};

//Here we convert our object to json
const bookJson = JSON.stringify(book);
console.log(bookJson);
//Here we convert the object back to a js object.
const parsedBook = JSON.parse(bookJson);
console.log(parsedBook.author);
